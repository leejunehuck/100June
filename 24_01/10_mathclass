#include <stdio.h>


int main()
{
	int a, b, c; //첫번째 줄
	int d, e, f; // 두번째 줄
	scanf_s("%d %d %d %d %d %d",&a,&b,&c,&d,&e,&f);
	for (int x = -999; x<=999; x++)
	{
		for (int y = -999;y<=999; y++)
		{
			if (c == a * x + b * y && f == d * x + e * y)
			{
				printf("%d %d", x, y);
			}
		}
	}

	return 0;
}


